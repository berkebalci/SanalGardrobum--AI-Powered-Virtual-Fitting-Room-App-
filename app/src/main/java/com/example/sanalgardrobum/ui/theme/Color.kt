package com.example.sanalgardrobum.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================================
// Tasarım kaynağından (Tailwind CSS) çıkarılan renk token'ları.
// Her değer, orijinal TSX dosyalarındaki kullanım yerine referans verir.
// ============================================================================

// ── Purple Palette ──────────────────────────────────────────────────────────
// Background gradient başlangıcı, containerColor, subtle highlight
val Purple50 = Color(0xFFFAF5FF)
val Purple100 = Color(0xFFF3E8FF)
val Purple400 = Color(0xFFC084FC)   // Feature card gradient start
val Purple500 = Color(0xFFA855F7)   // Accent gradient start, scanning effect
val Purple600 = Color(0xFF9333EA)   // Primary CTA gradient start, active tab
val Purple700 = Color(0xFF7E22CE)   // Wardrobe season badge text

// ── Pink Palette ────────────────────────────────────────────────────────────
val Pink50 = Color(0xFFFDF2F8)      // Background gradient middle
val Pink400 = Color(0xFFF472B6)     // Feature card gradient end
val Pink500 = Color(0xFFEC4899)     // Accent gradient end, heart fill
val Pink600 = Color(0xFFDB2777)     // Primary CTA gradient end

// ── Blue Palette ────────────────────────────────────────────────────────────
val Blue50 = Color(0xFFEFF6FF)      // Background gradient end, tip card bg
val Blue400 = Color(0xFF60A5FA)     // Feature card gradient start (blue-cyan)
val Blue500 = Color(0xFF3B82F6)     // Info card bg, tip icon bg

// ── Cyan Palette ────────────────────────────────────────────────────────────
val Cyan400 = Color(0xFF22D3EE)     // Feature card gradient end (blue-cyan)
val Cyan500 = Color(0xFF06B6D4)     // Info card gradient end

// ── Green Palette ───────────────────────────────────────────────────────────
val Green50 = Color(0xFFF0FDF4)     // Usage tag bg
val Green400 = Color(0xFF4ADE80)    // Feature card gradient start (green-emerald)
val Green500 = Color(0xFF22C55E)    // Success badge bg, completed step
val Green600 = Color(0xFF16A34A)    // Score bar green variant
val Green700 = Color(0xFF15803D)    // Usage tag text

// ── Emerald Palette ─────────────────────────────────────────────────────────
val Emerald400 = Color(0xFF34D399)  // Feature card gradient end (green-emerald)

// ── Orange Palette ──────────────────────────────────────────────────────────
val Orange400 = Color(0xFFFB923C)   // Feature card gradient start (orange-red)

// ── Red Palette ─────────────────────────────────────────────────────────────
val Red50 = Color(0xFFFEF2F2)       // Logout button bg
val Red400 = Color(0xFFF87171)      // Feature card gradient end (orange-red)
val Red500 = Color(0xFFEF4444)      // Delete icon bg
val Red600 = Color(0xFFDC2626)      // Logout text

// ── Yellow Palette ──────────────────────────────────────────────────────────
val Yellow400 = Color(0xFFFACC15)   // Star rating fill

// ── Amber Palette ───────────────────────────────────────────────────────────
val Amber400 = Color(0xFFFBBF24)    // Score breakdown (mevsimsellik)
val Amber600 = Color(0xFFD97706)    // Amber score gradient end

// ── Indigo Palette ──────────────────────────────────────────────────────────
val Indigo600 = Color(0xFF4F46E5)   // Style badge (formal)

// ── Teal Palette ────────────────────────────────────────────────────────────
val Teal500 = Color(0xFF14B8A6)     // Style badge (casual)

// ── Gray Palette ────────────────────────────────────────────────────────────
// UI'da en çok kullanılan scale — arka plan, border, metin tonları
val Gray50 = Color(0xFFF9FAFB)      // Input bg, subtle surface
val Gray100 = Color(0xFFF3F4F6)     // Divider, disabled button, bottom border
val Gray200 = Color(0xFFE5E7EB)     // Disabled CTA bg, border
val Gray300 = Color(0xFFD1D5DB)     // Toggle off track
val Gray400 = Color(0xFF9CA3AF)     // Inactive tab, disabled text, icon muted
val Gray500 = Color(0xFF6B7280)     // Secondary/tertiary text
val Gray600 = Color(0xFF4B5563)     // Body text, filter chip text
val Gray700 = Color(0xFF374151)     // Icon default tint, subtitle text
val Gray800 = Color(0xFF1F2937)     // Primary text, headings
val Gray900 = Color(0xFF111827)     // Darkest text, siyah referans